# 学生信息管理系统 - 云端部署笔记

> 日期：2026-07-28  
> 最后更新：2026-07-28（第二轮改进）

---

## 一、项目概述

- **项目路径**：`C:\Users\Kirito\Desktop\人工智能学院学生信息管理系统\student-system\`
- **GitHub 仓库**：https://github.com/DYZ133/JAI-
- **技术栈**：Node.js (Express) + Vue.js 2 + Element UI
- **数据存储**：`data.json`（JSON 文件数据库）
- **本地端口**：3456

## 二、本次改动的文件

| 文件 | 改动说明 |
|---|---|
| `server.js` | 端口改为 `process.env.PORT \|\| 3456`，新增 `/api/data/export`（导出）和 `/api/data/import`（导入）API，绑定 `0.0.0.0` |
| `package.json` | 添加 `"start": "node server.js"` 脚本，添加 `"engines": {"node": ">=16.0.0"}` |
| `public/index.html` | 仪表盘新增「💾 数据管理」卡片，包含「导出数据备份」和「导入数据恢复」按钮 |
| `.gitignore` | 新建，排除 `node_modules/`、`.claude/`、`.DS_Store`、`*.log` |
| `render.yaml` | 新建，Render.com 蓝图部署配置 |

## 三、本地运行

```bash
cd student-system
npm start
# 访问 http://localhost:3456
```

## 四、Git 操作

```bash
cd student-system

# 查看状态
git status

# 添加并提交
git add -A
git commit -m "描述信息"

# 推送到 GitHub（使用令牌）
git push https://DYZ133:你的令牌@github.com/DYZ133/JAI-.git master
```

### GitHub 令牌
- 创建地址：https://github.com/settings/tokens
- 权限：勾选 `repo`
- 令牌格式：`ghp_` 开头

## 五、Render.com 部署步骤

1. 打开 https://render.com → 用 GitHub 登录
2. 点击 **"New +"** → **"Blueprint"**
3. 连接仓库 `DYZ133/JAI-`
4. Render 自动读取 `render.yaml`，配置：
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: Free
5. 点击 **"Apply"**，等待部署
6. 获得公网 URL：`https://student-system-xxxx.onrender.com`

## 六、数据管理

### 导出备份
1. 打开系统 → 仪表盘
2. 点击「导出数据备份」
3. 浏览器自动下载 `data-backup-日期.json`

### 导入恢复
1. 打开系统 → 仪表盘
2. 点击「导入数据恢复」
3. 选择之前导出的 JSON 文件
4. 确认覆盖后自动刷新

> ⚠️ Render 免费版重新部署会重置 `data.json`，部署前后记得用导出/导入功能备份恢复！

## 七、API 列表（供参考）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/dashboard/stats` | 仪表盘统计 |
| GET/POST/PUT/DELETE | `/api/student/info/...` | 学生 CRUD |
| GET/POST/PUT/DELETE | `/api/student/grade/...` | 成绩 CRUD |
| GET/POST/PUT/DELETE | `/api/student/dormitory/...` | 宿舍 CRUD |
| GET/POST/PUT/DELETE | `/api/student/dormitory/assignment/...` | 住宿分配 |
| GET/POST/PUT/DELETE | `/api/student/class/...` | 班级 CRUD |
| GET/POST/PUT/DELETE | `/api/student/course/...` | 课程 CRUD |
| GET | `/api/data/export` | 导出全部数据 (JSON) |
| POST | `/api/data/import` | 导入数据覆盖 |

## 八、注意事项

1. **免费套餐限制**：Render 免费版 750 小时/月，15 分钟无访问会休眠（首次访问需等待唤醒）
2. **数据持久性**：部署时文件系统重置，数据通过导入导出备份
3. **HTTPS**：Render 自动提供 HTTPS 证书
4. **域名**：默认域名格式 `xxx.onrender.com`，可在 Render 设置中绑定自定义域名

## 九、第二轮改进（2026-07-28）

| 改动 | 说明 |
|---|---|
| 页面自动加载 | 新增 `watch` 监听 `page` 变化，切换侧边栏时自动请求对应模块数据 |
| 成绩搜索增强 | 成绩管理新增「学号」和「学生姓名」搜索字段，后端同步支持 |
| 宿舍分页 | 宿舍列表和住宿分配列表增加分页组件，解决数据量大时的性能问题 |

### 修改文件清单

| 文件 | 改动说明 |
|---|---|
| `public/index.html` | 新增 `watch.page`、成绩搜索字段、宿舍/分配分页 |
| `server.js` | `/api/student/grade/list` 新增 `studentNo` 和 `studentName` 查询参数 |
